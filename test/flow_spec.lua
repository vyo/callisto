describe('Flow module', function ()

  local flow = require 'flow'
  flow = flow()

  it('should expose mark, jump and callback functions', function ()
    pending('')
  end)

  it('should store the location of marked labels', function ()
    pending('')
  end)

  it('should jump to specified label', function ()
    pending('')
  end)

  it('should jump back to the originally next instruction after a call', function ()
    pending('')
  end)

end)
