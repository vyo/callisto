describe('Callisto', function ()

  local callisto = require 'callisto'
  callisto = callisto

  it('should expose functions to run, convert, export and weave', function ()
    pending('')
  end)
end)
