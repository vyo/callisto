describe('Interpreter', function ()

  local interpreter = require 'interpreter'
  interpreter = interpreter()

  it('should expose functions to loadCode, execute arbitrary numbers\n'..
    'of steps, dumpDataStack, dumpInstructionStack, dumpProgramCounter\n'..
    'and reset', function ()
    pending('')
  end)

end)
